# Security Policy

Fan Control 会以 root 权限安装一个本地 helper，并直接向 Apple SMC 写入风扇控制值。安全问题请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下提交，避免在公开 Issue 中披露可利用细节。

当前只维护最新发布版本。报告应包含受影响版本、macOS 与硬件型号、复现步骤，以及能够说明影响范围的日志。请删除日志中的用户名、主目录路径和其他个人信息。

普通功能缺陷、传感器兼容性问题和功能建议可以使用公开 Issue。
